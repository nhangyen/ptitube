/**
 * ActionTab — placeholder cho nút Create ở giữa tab bar.
 * Không render gì cả vì nút Create được thay thế bởi `CreateTabButton` trong TabLayout.
 */
export default function ActionTab() { return null; }